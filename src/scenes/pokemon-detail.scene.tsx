import { PokemonDetailContainer } from "pods/pokemon-detail/pokemon-detail.container";
import React from "react";

export const PokemonDetailScene: React.FunctionComponent = () => {
    return(
        <PokemonDetailContainer />
    )
}