import React from "react";
import { PokemonListContainer } from "pods/pokemon-list/pokemon-list.container";

export const PokemonListScene: React.FunctionComponent = () => {
    return <PokemonListContainer />
}